name: validate-cefr-tags

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'packages/core/data/wordlist.json'
      - 'packages/core/data/connected_speech.json'
      - 'packages/core/data/weak_forms.json'
      - 'tools/validate/validate-cefr-tags.py'
      - '.github/workflows/validate-cefr-tags.yml'
  push:
    branches: [main, master]
    paths:
      - 'packages/core/data/wordlist.json'
      - 'packages/core/data/connected_speech.json'
      - 'packages/core/data/weak_forms.json'
      - 'tools/validate/validate-cefr-tags.py'
      - '.github/workflows/validate-cefr-tags.yml'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Validate CEFR tags
        run: python3 tools/validate/validate-cefr-tags.py
