# CD 更新指示書(追補)— PC 3b-pc を app-5 決定に整合(2026-07-27)

- メタ: **部分更新**(追補 2 項目 CD-7 / CD-8)/ 規模: **小(pc.dc.html #3b-pc のみ)**
- 対象ファイル: `docs/claude-design/pc.dc.html`(`#3b-pc` のみ)
- 根拠: PARITY-CATALOG.md `§PC-conflicts` + Naoya 判断(2026-07-27, app-5 PC)
- 適用者: Naoya(Claude Design セッション、round-trip 維持)。※純粋削除のため ClaudeCode 直接パッチも可

---

## 1. ゴール

app-5(語彙リスト)の SP は CD `#3b` に完全準拠で実装・検証済。**PC(`#3b-pc`)に残る 2 要素が確定事項と矛盾**するため、CD `#3b-pc` を実装の正に合わせて是正する。app 側は既に本決定どおり(変更不要)。

## 2. 背景(矛盾点)

- CD `#3b-pc` の各単語カードに **SRS「覚えた 2/3」横3ボックス**が残存。→ **Q7 で「語彙リスト=発音辞書、SRS は 2a–2d 答え合わせ + 3d のみ」と確定**。SP CD `#3b` は正しく SRS 無し。PC CD が Q7 未反映。
- CD `#3b-pc` 上部に **●●● ウィンドウ chrome**(`<span>` 3点)。→ **app-4 で PC の ●●● は「R2 フレーム装飾の誤踏襲」として削除済**。CD-PC も装飾を持たない方針に統一。

## 3. Naoya 判断(2026-07-27)

- SRS: **Q7 準拠(SRS 無し)+ CD 修正** → CD `#3b-pc` から SRS ボックス削除。
- ●●●: **削除維持 + CD 修正** → CD `#3b-pc` から ●●● 削除。
- レイアウト: **まず単一カラムで確定**(2カラム化は保留。今回のCD修正対象外)。

## 4. 修正項目

### CD-7【削除】3b-pc 単語カードの SRS「覚えた」ボックス
- 対象: `pc.dc.html` `#3b-pc` の 4 枚の単語カード(jacket / apple / measure / through)
- 内容: 各カード末尾の「覚えた <b>N/3</b> + 横3チェックボックス」ブロック(`<div ...>覚えた ...</div>` 相当、chip 群含む)を **削除**。カードは 語+CEFR+品詞+意味+GA行/RP行 で終える(= SP `#3b` カードと同型)。
- 根拠: Q7。SP `#3b` に SRS が無いのと同じ状態にする。

### CD-8【削除】3b-pc ●●● ウィンドウ chrome
- 対象: `pc.dc.html` `#3b-pc` カード上部の高さ44px chrome 行内の `●●●`(`<span style="width:11px;height:11px;border-radius:999px;background:#E7DCCF">` ×3)
- 内容: 3点を **削除**(chrome 行自体を残すか除くかは他 PC フレームの CD-2 相当処理に合わせる。app では PC の `.modal-chrome` を display:none 済)。
- 根拠: R2 / app-4。装飾であり実 UI でない。

## 5. 保持事項(変更しない)

- `#3b-pc` の **コンパクト設計**(IPAで絞り込み・最大3・GA/RP 個別発音・意味表示)= 正。
- 2カラムグリッド構造・A–Z・「記号ピッカーを開く」ボタン等の**レイアウトは今回対象外**(単一カラム確定のため据え置き。将来 2カラム化する場合は別途)。
- Design System トークン・文言。

## 6. 適用後の確認

- `#3b-pc` のカードに「覚えた」ボックスが無い / 上部 ●●● が無い。
- SP `#3b` と PC `#3b-pc` のカードが同型(SRS 無し)であること。
