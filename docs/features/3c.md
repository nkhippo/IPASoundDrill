# 3c IPA 記号ピッカー

索引: `docs/features/README.md`。

## 観測可能挙動

- hash `#/vocab/ipa`（`ROUTES["vocab/ipa"]` → `showSymbolPickerView()`）。exclusive full-page（`body.symbol-picker-page`、`#symbolPickerPage`）
- 上段 IPA chart パレット（音声学的分類）+ query chips / 下段 live 部分一致結果（highlight）
- Query builder: 記号タップ蓄積（順序保持、最大 3 記号 `symbolQueryPush`）+ chip 個別削除
- **既知の乖離（2026-07-29 突合時点）**: `3b` の絞り込み UI は `.vocab-ipa-filter`（IPA キーボードをインライン統合、`vocabIpaFilterBar`）に置き換わっており、`#/vocab/ipa` へ遷移する UI トリガー（旧 Sticky filter の Segmented「IPA」ボタン等）は `apps/web/src/index.template.html` 内に見当たらない。ルート定義・DOM・関数 (`showSymbolPickerView`) 自体はコード上に温存されているが、現行 UI からは到達不能な可能性が高い（hash 直打ちでのみ到達）。要 Naoya 確認・別 Issue で扱う

## 画面構造

`#symbolPickerPage`（`body.symbol-picker-page`）

| 要素 | 内容 |
|------|------|
| Split view | 上段パレット固定 / 下段結果スクロール（仮想化） |
| Query chips | `symbolQuery` 配列。記号タップで蓄積・順序保持・個別削除 / Clear |
| IPA chart | `symbolChartGroups`（標準分類）。見出し EN primary + L1 sub |
| Live 検索 | 単純 `includes` 全走査。一致箇所は highlight |

## 採点則・定数

該当なし（検索・閲覧画面）。

## 読むデータ

`packages/core/data/wordlist.json`（公開 URL `/data/wordlist.json`）（IPA 記号の抽出元）。音素解説は `packages/core/i18n/phonemes/{lang}.json`（公開 URL `/i18n/phonemes/{lang}.json`）（`docs/data-contract.md` §5 i18n スキーマ）。

## i18n キー群

`symbol.picker.*` / `symbol.group.*.{en,sub}` / `symbol.height.*.{en,sub}`。

## 関連シンボル

`docs/impact-ledger.json` で `feature_ids` に `"3c"` を含むシンボルを参照（symbol 昇順の JSON 配列。scope・caller_areas・行番号を含む。スキーマは `docs/impact-ledger.md`）。
