# 3h このアプリについて

索引: `docs/features/README.md`。Footer への配置は `docs/features/_common.md`。

## 観測可能挙動

- トップページ末尾に DOM 常時配置（AI クローラビリティ。JS 無効時も見出しを保持する）
- リード、「なぜ IPA を学ぶか」、特徴 5 項目、フィードバック導線で構成する

## 画面構造

`#aboutBlock`（フッター常時 DOM。`docs/features/_common.md` の Footer 参照）。IPA 表記は `--font-ipa`、その他は Mood B token を使用する。

## 採点則・定数

該当なし。

## 読むデータ

なし（静的 UI 文言のみ）。

## i18n キー群

`about.lead`（text）/ `about.why_ipa_html` / `about.features.item_1_html`〜`item_5_html` / `about.contact_html`（HTML として適用）/ `about.title` / `about.placeholder`。

## 関連シンボル

Issue F の impact-ledger 生成後にリンク。
