# 1a トップページ

索引: `docs/features/README.md`。共通シェル・セッションフローは `docs/features/_common.md`。

## 観測可能挙動

- エントリーポイント。目的 4 カードで即開始できる
- 目的カードタップ → `applyDrillId` → `3a` 学習プロフィールへ遷移（セッションフローは `docs/features/_common.md`）
- フッターに `3h`「このアプリについて」を DOM 常時配置（AI クローラビリティ。JS 無効時も見出しを保持）

## 画面構造

| 領域 | 内容 |
|------|------|
| ヘッダー | 言語切替（`#langSwitcher` / `#langMenu`）+ ガイドアイコン + 語彙ボタン（詳細は `docs/features/_common.md`） |
| Hero | タグラインのみ（`top.tagline`、`--font-serif`）。サブコピー・独立 CTA なし |
| 目的 4 カード | `.purpose-card`、`drill.title.2a`–`2d` |
| フッター | Feedback / Terms / Privacy / X + `3h` 導線（`docs/features/_common.md` Footer 参照） |

Font family トークン: `--font-ui` / `--font-serif` / `--font-ipa` / `--font-mono`（詳細は `docs/design/phase-1/visual-tokens.md`）。

## 採点則・定数

該当なし（エントリー画面。採点は行わない）。

## 読むデータ

なし（静的 UI 文言のみ。目的カードのタップは `2a`–`2d` へ遷移するのみ）。

## i18n キー群

`top.tagline` / `drill.title.2a`〜`2d`。共通シェルの i18n は `docs/features/_common.md`。

## 関連シンボル

`docs/impact-ledger.json` で `feature_ids` に `"1a"` を含むシンボルを参照（symbol 昇順の JSON 配列。scope・caller_areas・行番号を含む。スキーマは `docs/impact-ledger.md`）。
